{
  "mcpServers": {
    "plugin-forge": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}",
        "python",
        "-m",
        "plugin_forge.mcp_server"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
