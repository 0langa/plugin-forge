# Import Preservation

The first import preserves behavior. Unknown provider fields belong in provider extras.

| Found surface | Preserve as |
| --- | --- |
| skills directory | skill surfaces |
| command files | command surfaces |
| hook config | hook surfaces or provider extras |
| MCP config | MCP surfaces or provider extras |
| unknown manifest keys | provider extras |

