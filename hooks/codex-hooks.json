{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=\"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-$PWD}}\"; uv run --project \"$root\" python \"$root/scripts/run_codex_hook.py\" session_start",
            "commandWindows": "$root = if ($env:PLUGIN_ROOT) { $env:PLUGIN_ROOT } elseif ($env:CLAUDE_PLUGIN_ROOT) { $env:CLAUDE_PLUGIN_ROOT } else { (Get-Location).Path }; uv run --project $root python (Join-Path $root 'scripts/run_codex_hook.py') session_start"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=\"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-$PWD}}\"; uv run --project \"$root\" python \"$root/scripts/run_codex_hook.py\" post_tool_use",
            "commandWindows": "$root = if ($env:PLUGIN_ROOT) { $env:PLUGIN_ROOT } elseif ($env:CLAUDE_PLUGIN_ROOT) { $env:CLAUDE_PLUGIN_ROOT } else { (Get-Location).Path }; uv run --project $root python (Join-Path $root 'scripts/run_codex_hook.py') post_tool_use"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "root=\"${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-$PWD}}\"; uv run --project \"$root\" python \"$root/scripts/run_codex_hook.py\" user_prompt_submit",
            "commandWindows": "$root = if ($env:PLUGIN_ROOT) { $env:PLUGIN_ROOT } elseif ($env:CLAUDE_PLUGIN_ROOT) { $env:CLAUDE_PLUGIN_ROOT } else { (Get-Location).Path }; uv run --project $root python (Join-Path $root 'scripts/run_codex_hook.py') user_prompt_submit"
          }
        ]
      }
    ]
  }
}
