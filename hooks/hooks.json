{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "py -3 -c \"import os,runpy,sys; root=os.environ.get('CLAUDE_PLUGIN_ROOT') or os.environ.get('PLUGIN_ROOT') or os.getcwd(); sys.path.insert(0, os.path.join(root,'src')); runpy.run_path(os.path.join(root, 'src/plugin_forge/hooks/session_start.py'), run_name='__main__')\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "py -3 -c \"import os,runpy,sys; root=os.environ.get('CLAUDE_PLUGIN_ROOT') or os.environ.get('PLUGIN_ROOT') or os.getcwd(); sys.path.insert(0, os.path.join(root,'src')); runpy.run_path(os.path.join(root, 'src/plugin_forge/hooks/post_tool_use.py'), run_name='__main__')\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "py -3 -c \"import os,runpy,sys; root=os.environ.get('CLAUDE_PLUGIN_ROOT') or os.environ.get('PLUGIN_ROOT') or os.getcwd(); sys.path.insert(0, os.path.join(root,'src')); runpy.run_path(os.path.join(root, 'src/plugin_forge/hooks/user_prompt_submit.py'), run_name='__main__')\""
          }
        ]
      }
    ]
  }
}
